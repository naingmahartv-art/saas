// Intentionally empty. The renderer just displays the existing Next.js web
// app, which talks to its own API routes over HTTP as it always has — no
// Node/Electron APIs need to be exposed to the page.
